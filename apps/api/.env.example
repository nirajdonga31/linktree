# ============================================
# API Backend Environment Configuration
# ============================================

# Firebase Admin SDK Service Account (Private - keep secret!)
# Get this from: Firebase Console → Project Settings → Service Accounts → Generate new private key
# Then paste the entire JSON content (minified to single line) here
FIREBASE_CONFIG=

# Server Port
PORT=3001
